import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
    tableHeader: {
        backgroundColor: '#000'
    },
    tableContainer: {
        maxHeight: '74vh',
    },
    statusBtns: {
        alignItems: 'center',
        justifyContent: 'center',
        alignSelf: 'center',
    }
}));
