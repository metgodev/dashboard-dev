export const DEFAULT_LOCATION = { lat: 32.0713, lng: 34.7886 }
export const DEFAULT_ZOOM = 15;
export const { innerWidth: windowWidth, innerHeight: windowHeight } = window
export const DEFAULT_CONTANER_STYLE = { width: windowWidth, height: windowHeight / 2 }