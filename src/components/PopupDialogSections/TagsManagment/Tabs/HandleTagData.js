export const Picker = {
    tagCategories: []
}

export const GetFormData = (values) => {

    // let formValues = {
    //     categoryId: values.length || Object.keys(values).length ? values.hasOwnProperty('phoneNumber') ? values.phoneNumber : initialData.phoneNumber : initialData.phoneNumber,
    //     tagId: values.length || Object.keys(values).length ? values.hasOwnProperty('email') ? values.email : initialData.email : initialData.email,
    // }

    // const [returnValues, setReturnValues] = useState(formValues)

    // useEffect(() => {
    //     setReturnValues(formValues)
    // }, [values])

    // return returnValues
}